# Scenario: MRGFY-8448 analysis — inplace freshening on a gh-stack bottom.
# max_parallel_checks: 1 forces inplace checks so a commits-behind bottom
# triggers the REST update-branch call.
merge_queue:
  max_parallel_checks: 1
queue_rules:
  - name: default
    merge_conditions: []
    merge_method: squash
