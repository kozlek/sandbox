# Scenario: gh-stack bottom-merge via merge-async (GITHUB_STACK_BOTTOM_QUEUE_FOR_ORGS).
# Entry via `@mergifyio queue` command only; no conditions so embarked PRs merge immediately.
queue_rules:
  - name: default
    merge_conditions: []
    merge_method: squash
